using System;

namespace ToolBox.Models
{
    internal class TimeItem
    {
        private DateTime now;

        public TimeItem(DateTime now)
        {
            this.now = now;
        }
    }
}