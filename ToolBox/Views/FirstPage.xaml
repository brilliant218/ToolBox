<Page
    x:Class="ToolBox.Views.FirstPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToolBox.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding FirstPageInstance, Source={StaticResource Locator}}" 
    >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Horizontal">
            <Button Content="To SecondPage" Command="{Binding NavigationCommand}" Width="auto" Height="auto" />
            <Button Content="Back" Command="{Binding NavigationBackCommand}" Width="auto" Height="auto" Margin="20,0"/>
        </StackPanel>
    </Grid>
</Page>

